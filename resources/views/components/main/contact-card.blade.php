<div class="flex items-center gap-4">
    <i class="w-9 {{ $iconClass }}"></i>
    <div>
        <h3 class="uppercase text-accent font-medium font-secondary tracking-widest text-xl mb-2">{{ $heading }}</h3>
        <strong>{{ $slot }}</strong>
    </div>
</div>
